'use strict';

document.getElementById('newNoteButton').addEventListener('click', function () {
  var note = document.createElement('div');
});

var alertFunc = function alertFunc(text) {
  return alert(text);
};

alertFunc("omg, it worked!");